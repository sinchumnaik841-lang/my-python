# divisibility test
num=int(input("enter the number to checker: "))
divisor=int(input)("enter the divisor:")
if divisor==0:
  print("divisor cannot be zero!")
else:
     if num%divisor==0:
      print(f"{num} is divisible by {divisor}.")
else:
     print(f"f{num} is not divisible by {divisor}.")
